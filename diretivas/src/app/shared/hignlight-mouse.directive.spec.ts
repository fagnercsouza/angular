import { HignlightMouseDirective } from './hignlight-mouse.directive';

describe('HignlightMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new HignlightMouseDirective();
    expect(directive).toBeTruthy();
  });
});
